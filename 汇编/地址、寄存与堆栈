#1.
  对于  MOV EAX,[EBX]
  指的是将[]中的地址所储存的值赋给eax
#2.
  有时需要使用的变量值较多，寄存器数量不够，可以将值压入堆栈
  
#3.关于堆栈
  a.我们使用堆栈指针寄存器ESP用来储存当前栈顶的地址
  b.当我们将值压入栈中时，栈中的地址是由高地址向低地址增长的，
  c.在此过程中，堆栈指针寄存器ESP为保持指向栈顶，会首先减去相应的储存单元的宽度，
    然后才将数据送入ESP指定的储存单元
  d.入栈指令PUSH的操作数可以是32、16位寄存器 字、双字储存单元或立即数
    出栈指令POP的操作数不能是立即数或代码段寄存器CS
    出入栈指令至少对16位数据起作用
